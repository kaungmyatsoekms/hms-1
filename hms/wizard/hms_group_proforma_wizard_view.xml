<odoo>
    <data>
        <record id="group_proforma_invoice_wizard_form" model="ir.ui.view">
            <field name="name">hms.group.proforma.send.form</field>
            <field name="model">hms.group.proforma</field>
            <field name="groups_id" eval="[(4,ref('base.group_user'))]"/>
            <field name="arch" type="xml">
                <form string="Proforma Invoice">
                    <!-- truly invisible fields for control and options -->
                    <field name="composition_mode" invisible="1"/>
                    <field name="email_from" invisible="1" />
                    <field name="mail_server_id" invisible="1"/>
                    <div name="mail_form">
                    <!-- attrs="{'invisible': [('is_email', '=', False)]}" -->
                        <!-- visible wizard -->
                        <div attrs="{'invisible': [('composition_mode', '=', 'mass_mail')]}">
                            <group>
                                <label for="partner_ids" string="Recipients" groups="base.group_user"/>
                                <div groups="base.group_user">
                                    <span attrs="{'invisible': [('composition_mode', '!=', 'mass_mail')]}">
                                        <strong>Email mass mailing</strong> on
                                        <span>the selected records</span>
                                    </span>
                                    <span>Followers of the document and</span>
                                    <field name="partner_ids" widget="many2many_tags_email" placeholder="Add contacts to notify..."
                                        context="{'force_email':True, 'show_email':True}" attrs="{'invisible': [('composition_mode', '=', 'mass_mail')]}"/>
                                </div>
                                <field name="subject" placeholder="Subject..." attrs="{'required': [('composition_mode', '=', 'comment')]}"/>
                            </group>
                            <field name="body" style="border:none;" options="{'style-inline': true}"/>
                        </div>
                        <group>
                            <group attrs="{'invisible': [('composition_mode', '=', 'mass_mail')]}">
                                <field name="attachment_ids" widget="many2many_binary" string="Attach a file" nolabel="1" colspan="2" attrs="{'invisible': [('composition_mode', '=', 'mass_mail')]}"/>
                            </group>
                            <group>
                                <field name="template_id" options="{'no_create': True, 'no_edit': True}"
                                    context="{'default_model': 'hms.reservation'}"/>
                            </group>
                        </group>
                    </div>

                    <footer>
                        <button string="Send"
                            name="action_group_proforma" type="object" class="send_and_print btn-primary o_mail_send"/>
                        <button string="Cancel" class="btn-secondary" special="cancel" />
                    </footer>
                </form>
            </field>
        </record>

       <act_window
            name="action_group_proforma"
            id="confirm_letter"
            res_model="hms.group.proforma"
            binding_model="hms.reservation.line"
            binding_views="list"
            view_mode="form"
            target="new"
            context="{
                'default_template_id': ref('hms.group_proforma_template')}"/>

    </data>
</odoo>